spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/backend_cms
        username: ''
        password: ''
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
    servlet:
        multipart:
            max-file-size: 2GB
            max-request-size: 2GB
video:
    path: /Users/fvillanueva/Documents/videos/
rsa:
    private-key: classpath:certs/private.pem
    public-key: classpath:certs/public.pem
